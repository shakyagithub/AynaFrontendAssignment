interface messageType {
    id: number,
    message: string,
    createdOn: string,
    userId: number
}

interface userType {
    id: number,
    username: string,
    email: string,
    password: string
}